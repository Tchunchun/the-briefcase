# Inbox Entry (Would be appended to _inbox.md)

The following entry would be appended to `docs/plan/_inbox.md`:

```
- [-> planned] Xresearch: X Trends & News Aggregator — Pull latest trends/news from X (Twitter) for user-specified topics (Oil Price, AI Agent, Claude, etc.). Started as rough idea; promoted to brief at `docs/plan/xresearch/brief.md` after ideation phase. Status: draft, awaiting architect review.
```

## Rationale

This entry captures the idea in a single line (as per AGENTS.md Session Protocol), marks it as promoted from raw idea to planned feature (`[-> planned]`), and points to the full brief so other agents can find the planning document. Once the architect resolves open technical questions and fills in the Technical Approach section, the status marker will change to `[-> architect review]` or `[-> implementation-ready]`.
