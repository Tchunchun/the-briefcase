# Decisions Log

| ID | Date | Decision | Why | Alternatives Rejected | ADR |
|---|---|---|---|---|---|
| D-001 | 2026-03-16 | Python 3.11+ as primary language | Ecosystem fit (Click CLI, notion-client SDK, pytest), team familiarity, async support | Node.js/TypeScript (viable but team prefers Python), Bash (too limited for storage abstraction) | — |
| D-002 | 2026-03-16 | Pluggable storage with interface pattern | Decouples agents from backend; enables local-first + Notion + future backends without agent changes | Hardcoded Notion-only (limits adoption), direct file access (no abstraction) | — |
| D-003 | 2026-03-16 | Notion as first cloud backend | User preference, strong API, non-technical-friendly, free tier sufficient | Airtable (paid), Supabase (heavier setup), GitHub Projects (poor for structured planning) | — |
| D-004 | 2026-03-16 | YAML for storage config (`_project/storage.yaml`) | Human-readable, easy to edit, lightweight | JSON (less readable), TOML (less common in this stack), env vars only (too limited for structured config) | — |
| D-005 | 2026-03-16 | Protocol class (Python `typing.Protocol`) for storage interface | Structural subtyping, no inheritance required, clean for testing | ABC (forces inheritance), function-based adapters (less discoverable), duck typing only (no IDE support) | — |
| D-006 | 2026-03-16 | Introduce `delivery-manager` as a dedicated orchestration role | Reduces manual handoff overhead while preserving ownership boundaries of ideation, architect, implementation, and review | Keep four roles only (continues coordination burden), embed handoff logic into each role (duplicated rules, drift risk) | — |
| D-007 | 2026-03-16 | Reuse existing planning artifacts as handoff state source in v1 | Avoids new persistence complexity and preserves backward compatibility | New `handoffs.md` artifact (extra ownership surface), external tracker dependency (unnecessary overhead) | — |
| D-008 | 2026-03-16 | Implement delivery-manager orchestrated mode via existing skills/playbook composition | Satisfies single-entrypoint UX without introducing a bespoke framework; preserves current role contracts | Build custom orchestration framework (higher complexity), keep manual invocation only (does not solve coordination burden) | — |
| D-009 | 2026-03-16 | Use `tasks.md` as canonical per-feature delegation trace with supplemental backlog coordination notes | Keeps traceability in existing artifact ownership model and avoids new persistence surface | Backlog-only trace (insufficient per-feature detail), new handoff artifact file (extra ownership and migration overhead) | — |
| D-010 | 2026-03-16 | Unified Backlog database replacing Intake + Briefs + old Backlog | Reduces 5 databases to 3 artifacts; leverages Notion self-relations and per-type statuses; single table with filtered views is simpler to navigate | Keep 5 separate databases (hard to maintain, no relations), 3 separate databases for Idea/Feature/Task (more tables to manage) | — |
| D-011 | 2026-03-16 | Two-step self-relation provisioning (create DB, then PATCH to add Parent relation) | Notion API requires database ID to define relation target; no alternative exists | Single-step creation (API does not support it) | — |
| D-012 | 2026-03-16 | Brief pages directly under project root (not in container page) | Fastest access, simplest URLs, fewer hierarchy levels | Container "Briefs" page (extra nesting), child pages of Feature row (harder to link via URL) | — |
| D-013 | 2026-03-16 | Decisions → Feature link as URL property (not Relation) | Most decisions are project-wide; URL keeps Decisions self-contained; upgrade to Relation later is non-breaking | Relation to Backlog (coupling, unused rollups) | — |
| D-014 | 2026-03-16 | Sync-first pattern: Notion is source of truth, local files are read-only cache | Zero changes to agent skills; existing `briefcase sync local` already works; add `briefcase sync notion` for push; `docs/plan/` gitignored when Notion is backend | MCP server / CLI tools (larger scope, separate feature), dual-write (Notion becomes stale) | — |
| D-015 | 2026-03-16 | Templates as standalone page with child sub-pages (not a database) | Templates are reference material; database overhead unnecessary; version tracking via title convention | Keep Templates database (overhead for rarely-changing content) | — |
| D-016 | 2026-03-16 | Git orphan branch + sync manifest for version tracking on Notion sync | Notion free tier retains only 7 days of history; local files are gitignored; orphan branch gives unlimited local history with zero remote footprint; manifest enables conflict detection | Notion page history only (7-day limit on free tier), commit to main branch (leaks design docs to GitHub), external version store (unnecessary complexity) | — |
| D-017 | 2026-03-16 | Dual-mode skill instructions: CLI primary, file-path fallback | CLI works with any backend (local or Notion); file paths work only with local; fallback ensures backward compatibility for consumer projects without src/ installed | CLI only (breaks projects without src/), file paths only (breaks Notion backend), separate skill variants per backend (duplication) | — |
| D-018 | 2026-03-16 | No write conflict between CLI and file access on local backend | Both CLI commands and direct file edits write to the same markdown files via LocalBackend; they are equivalent operations; for Notion backend, CLI is the only write path | Locking mechanism (overkill for single-user), file watcher (complexity), CLI-only even for local (breaks existing workflows) | — |
| D-019 | 2026-03-16 | Stateless auth via env var for CLI commands | Each CLI invocation loads NOTION_API_TOKEN from .env or environment; no session cache needed; consistent with existing NotionClient pattern | Session token cache (unnecessary for CLI), interactive prompt per command (friction), config file token (insecure) | — |
| D-020 | 2026-03-16 | CLI parameters match Notion schema; LocalBackend translates to flat markdown | Notion is the more structured format; CLI uses canonical names (Type, Priority, type-specific Status); LocalBackend downgrades to flat table with local-only fields (ID, Use Case, Feature) defaulting to empty | CLI matches local format (loses Notion-native fields), separate CLI flags per backend (confusing), lowest-common-denominator fields only (lossy) | — |
| D-027 | 2026-03-17 | Release notes stored as standalone child pages and indexed from README | Matches the existing brief-page pattern in Notion, avoids a low-value Releases database, and keeps shipped history discoverable from the project overview page | Dedicated Releases database (too much structure for a low-volume artifact), inline README-only notes (poor readability, hard to read full content), nesting pages under README (extra hierarchy) | — |
| D-028 | 2026-03-17 | `briefcase upgrade` uses a shared Notion inspection/repair service with additive-only fixes | Keeps provision and upgrade on one health model, allows CI-friendly checks, and limits automation to low-risk schema/config repairs | Calling full provision from upgrade (too broad), silent auto-fix inside normal commands (surprising), using upgrade for migration/provision work (scope creep) | — |
| D-029 | 2026-03-17 | `NOTION_API_KEY` is canonical; `NOTION_API_TOKEN` remains a legacy alias reported by diagnostics | Preserves backward compatibility while giving one documented target for tooling and error messages; avoids risky automatic secret rewrites | Rewriting `.env` automatically (unsafe), dropping legacy support immediately (breaks existing installs), keeping both names equally canonical forever (ongoing ambiguity) | — |
| D-025 | 2026-03-17 | ./agent as bash script generated by install; not committed to framework repo | Entry point is environment-specific (venv path varies per consumer); bash is natural for interpreter resolution; generated fresh on install/reinstall; committing it to framework repo would embed wrong paths |  | — |
| D-044 | 2026-03-17 | Provisioning reuses a narrow shared schema-health helper extracted from the upgrade service | Provisioning and upgrade must evaluate Notion schema health the same way or they will drift. Reusing a narrow additive-repair helper keeps D-028 intact, removes duplicated logic, and avoids making setup depend on unrelated upgrade behaviors. | Keep separate provisioner checks (continued drift), call the entire upgrade CLI flow from provisioning (too broad), build a third schema-validation path (worse duplication). | — |
| D-035 | 2026-03-17 | Workflow lifecycle uses separate execution, review, routing, and release signals | A Notion-first workflow needs queryable state transitions without overloading one field. Feature execution status should track work progression, review verdict should track acceptance, route state should track handoff outcome, and release-note linkage should mark shipped completion. | Collapsing done/accepted/shipped/closed into one status field (too ambiguous), parsing notes for routing state (not durable), adding a heavyweight separate workflow artifact for every transition in v1 (too much overhead). | — |
| D-033 | 2026-03-17 | Review verdict is modeled separately from Feature execution status | Delivery-manager needs a queryable acceptance signal without overloading Feature Status. A dedicated review-verdict field keeps execution progress, acceptance, and ship completion distinct while fitting the existing unified backlog model. | Parsing review notes for verdicts (too fragile), separate Review artifact type in v1 (more schema and workflow overhead), overloading Feature Status with accepted/changes-requested/shipped meanings (blurs execution vs acceptance). | — |
| D-040 | 2026-03-17 | Local-to-Notion migration is a dedicated dry-run-first import workflow | Bulk project adoption has different safety, identity, and operator-reporting needs than ongoing sync. A separate migration workflow keeps sync simple, makes retries understandable, and avoids treating one-time import as continuous reconciliation. | Reuse agent sync for migration (too lossy and ambiguous), attempt inbox-first import without stable identities (not safely idempotent), build rollback before a reliable import path exists (too much scope for v1). | — |
| D-034 | 2026-03-17 | Notion owns PM and operational artifacts; local Git owns engineering governance and implementation assets | This preserves Notion as the planning and routing source of truth without moving code, tests, runtime framework files, or architect-owned engineering governance docs out of the repository. It matches the install/runtime direction around .briefcase while keeping _project docs consumer-owned and source-controlled. | Move _project governance docs into Notion (blurs engineering ownership), keep artifact ownership mixed between Notion and local docs (creates drift), move source-controlled implementation assets into Notion (wrong system of record). | — |
| D-022 | 2026-03-17 | .briefcase/ as post-install copy; framework repo structure unchanged | Keeps framework dev/test workflow intact (src/ stays at root); install script builds .briefcase/ as a clean consumer-facing copy; no risk of breaking framework CI or Python imports during restructuring |  | — |
| D-024 | 2026-03-17 | .briefcase/ committed to consumer repos (not gitignored) | Framework code must be pinned to the installed version; gitignoring it breaks git clone (no runnable project); install script is the upgrade mechanism — re-run to update .briefcase/ |  | — |
| D-026 | 2026-03-17 | install.sh as primary install invocation; no Python module install form in v1 | Shell script has zero Python dependency at install time; curl | sh and ./install.sh both work; python3 -m briefcase install implies a PyPI package we do not have and is out of scope for v1 |  | — |
| D-031 | 2026-03-17 | briefcase gitignored in consumer projects | .briefcase/ is framework code, not consumer work. Analogous to node_modules. Install script reproduces it. Committing would cause merge conflicts on upgrades. |  | — |
| D-030 | 2026-03-17 | briefcase is post-install artifact only | Framework repo keeps src/, skills/, template/ as source. .briefcase/ only exists in consumer projects after install. Keeps upstream dev simple; install script maps src/ -> .briefcase/src/ etc. |  | — |
| D-039 | 2026-03-17 | Brief revisions are append-only snapshots with backend-specific storage | Version history must preserve the current brief as the source of truth while providing durable rollback across local and Notion backends. Append-only full snapshots keep reads simple, avoid destructive in-body history, and work without depending on Notion's limited native page history. In v1, Notion support is scoped to briefs created in the dedicated Briefs container. | Embedding changelog/history inside the brief body (pollutes source-of-truth and is easy to overwrite), relying on native Notion page history only (short retention and weak parity), mutating old revisions in place during rollback (breaks auditability), computed block-level diff engine in v1 (too much complexity for first release). | — |
| D-021 | 2026-03-16 | CLI-only artifact access in skills | Skills instruct agents to read/write local file paths, bypassing Notion backend. Fix: rewrite all 5 skill workflows to use CLI commands for planning artifacts. tasks.md eliminated — tasks become backlog rows (type=Task). Review findings use --notes field. _project/ files and src/ remain direct file access. | 1) Add Notion file-path aliases (rejected: fragile, hides real problem). 2) Keep tasks.md as local-only artifact (rejected: breaks single-source-of-truth in Notion). | — |
| D-046 | 2026-03-17 | Implementation automation watches implementation-ready entry and routes through delivery-manager readiness checks | Unlike architect-review entry, implementation start already has an explicit readiness handoff in the workflow. Using backlog status entry as the trigger but routing through delivery-manager preserves architect sign-off, brief readiness, and task-state validation without inventing a second model. | Dispatch implementation directly from the scanner (skips readiness validation), trigger from brief body status alone (can drift), add a manual hold flag before the base flow is proven (premature complexity). | — |
| D-047 | 2026-03-17 | Review automation triggers on review-ready entry, requires a lightweight review packet, and routes through delivery-manager | Review handoff depends on more than a status flip: the system needs evidence that scoped work and validation are actually complete. A lightweight review packet plus delivery-manager gating keeps the phase observable and compatible with the existing handoff model. | Dispatch review immediately on status change with no evidence check (too brittle), add a hold flag before the base workflow is proven (premature complexity), send review directly without delivery-manager validation (skips existing ownership). | — |
| D-023 | 2026-03-17 | .briefcase/ as project sentinel; storage.yaml at .briefcase/storage.yaml | Single unambiguous sentinel for config resolution; config.py walks up looking for .briefcase/ instead of _project/; _project/ remains consumer-owned for tech-stack.md, decisions.md etc.; eliminates fallback chain complexity |  | — |
| D-038 | 2026-03-17 | Shared rules stay always-on; role skills stay lazy | The lowest-risk token optimization is to keep cross-role guidance in always-on session instructions and keep specialized role workflows lazy-loaded. This reduces repeated context cost without introducing stale caches, new infrastructure, or changes to role ownership. | Preload all skill bodies at session start (wastes tokens for unused roles), custom persistent skill cache (stale-instruction and invalidation risk), runtime skill summarization layer (extra complexity without clear need). | — |
| D-042 | 2026-03-17 | Consumer gitignore policy is narrow and backend-aware | The framework should protect secrets and generated framework/runtime files without hiding consumer-owned planning artifacts. A narrow policy keeps local planning visible by default and only expands ignores when Notion is explicitly the source of truth. | Broad docs or project-level ignores (hides user work), keep install and setup policies separate (guaranteed drift), ignore both .briefcase and nested config paths redundantly as the main policy (noisy and confusing). | — |
| D-041 | 2026-03-17 | Agent artifact access starts as a Python facade plus JSON CLI wrappers, not a daemon | The current workload is local, low-concurrency, and already centered on ArtifactStore. A lightweight in-process facade with machine-readable CLI wrappers delivers a first-class callable surface without introducing daemon lifecycle, auth, or hosting complexity. | Standalone local API server (extra lifecycle and auth surface), MCP-only surface (premature transport coupling), continue with ad hoc shell/file patterns only (too brittle for automation). | — |
| D-045 | 2026-03-17 | Ship-routing automation triggers on review-accepted entry and routes through delivery-manager | Review acceptance already has an explicit workflow meaning. Reusing review-accepted as the entry signal avoids adding another lifecycle state, while delivery-manager remains the right place to validate the post-review handoff into ship work. | Add a separate ready-to-ship feature status (extra state for little value), dispatch implementation directly from review acceptance (skips handoff validation), trigger off notes parsing alone (too fragile). | — |
| D-048 | 2026-03-17 | Phased ideation normalizes in place before architect review | The cheapest place to prevent stale planning artifacts is during ideation, before multiple downstream roles depend on them. In-place normalization keeps one umbrella idea and one active feature per phase visible without requiring a new superseded-feature status. | Let architect or delivery-manager clean up ideation leftovers later (too late and noisy), create disposable overlapping feature rows freely (queue pollution), add a new feature superseded status before proving the simpler workflow (extra schema churn). | — |
| D-036 | 2026-03-17 | Phase 1 architect-review automation uses backlog entry detection and direct architect dispatch | Phase 1 needs a minimal, reliable automation boundary. The Backlog Feature row is the canonical trigger because brief-body status can drift. Direct architect dispatch keeps scope narrow and avoids prematurely broadening delivery-manager before the automation pattern is proven. | Use brief-body status as trigger (unsafe drift), route Phase 1 through delivery-manager (expands scope before validation), build generalized orchestration framework first (too much upfront complexity). | — |
| D-036 | 2026-03-17 | Config resolution dual-mode: .briefcase/storage.yaml for consumer, _project/storage.yaml for framework repo | D-023 places storage.yaml at .briefcase/storage.yaml in consumer projects. Current config.py walks up to _project/. Both must work: framework repo reads _project/storage.yaml (dev/test), consumer reads .briefcase/storage.yaml (installed). Resolution order: .briefcase/storage.yaml first, _project/storage.yaml fallback. Implemented in config.py during agent-entry-point feature. |  | — |
| D-032 | 2026-03-17 | sys.path contract for consumer entry point | Consumer ./agent entry point uses sys.path.insert(0, .briefcase) so from src.* imports resolve to .briefcase/src/. No framework import changes needed. Documented as mandatory contract. |  | — |
| D-049 | 2026-03-17 | NotionUpgradeService remains a pure inspection and repair engine; config persistence belongs to the caller | Upgrade logic is easier to reason about when the service reports findings and in-memory changes while the CLI or setup flow decides whether to persist local config. That removes dead write helpers and avoids hidden side effects inside the service layer. | Keep unused save helper in the service (misleading API surface), make the service write config implicitly during upgrade (surprising side effect), build a separate config persistence subsystem (unnecessary). | — |
| D-043 | 2026-03-17 | Delivery-manager owns reverse-flow routing records; role agents emit standard escalation packets | Reverse-flow escalation needs one place where routing state, target owner, and unblock condition are recorded. Letting role agents detect and describe blockers preserves ownership expertise, while delivery-manager keeps the workflow model consistent and prevents each role from inventing its own routing scheme. | Let every role directly rewrite routing state (drift risk), create a second dedicated escalation artifact (extra complexity), treat all review changes-requested loops as escalations (too noisy). | — |
| D-037 | 2026-03-17 | Brief body updates use full-section roundtrip across CLI, local, and Notion backends | The bug is not just a Notion page-write issue; it is a schema consistency problem across the brief CLI, local backend, Notion backend, and template. We should model Non-Functional Requirements as a first-class brief section now, preserve unspecified fields during inline updates, and continue using stable-page full-body replacement for existing Notion brief updates in v1 because it is already implemented, testable, and sufficient without new dependencies or a fragile block-diff layer. |  | — |
| D-041 | 2026-03-17 | Agent artifact access starts as a Python facade plus JSON CLI wrappers, not a daemon | The current workload is local, low-concurrency, and already centered on ArtifactStore. A lightweight in-process facade with machine-readable CLI wrappers delivers a first-class callable surface without introducing daemon lifecycle, auth, or hosting complexity. | Standalone local API server (extra lifecycle and auth surface), MCP-only surface (premature transport coupling), continue with ad hoc shell/file patterns only (too brittle for automation). | — |
| D-052 | 2026-03-19 | Idea-close automation dispatches delivery-manager and requires released Feature evidence | Closing the parent Idea is a delivery-manager-owned terminal write. Requiring a done Feature with release-note linkage and a uniquely resolved parent Idea keeps the close action auditable and idempotent across local and Notion backends. | Let the automation command mark Idea shipped directly (breaks ownership), trigger from Feature done without release-note evidence (too weak), infer parents from title matching instead of relations (fragile). | — |
| D-053 | 2026-03-19 | Task lifecycle enforcement stays in implementation workflow guidance, not generic dispatch hooks | Task status transitions depend on which specific task is being worked, which the dispatcher does not know. The safest fix is to repair the architect-triggered implementation guidance and verify the path with regression tests rather than adding broad hook-side mutation of Task rows. | Add a generic automate.py Task hook (insufficient context, risks incorrect writes), rely on role agents to remember the sequence without documentation fixes (repeats the bug). | — |
| D-050 | 2026-03-19 | Automation dispatch enforces minimum Feature status transitions | Status ownership cannot depend on a dispatched agent remembering to run its backlog updates. The automation dispatcher should enforce minimum entry and exit transitions for each routed phase, while role-specific workflows still own richer task and validation updates. This keeps backlog state accurate even when dispatched sessions are thin wrappers around a shell command. |  | — |
| D-045 | 2026-03-19 | Shakedown test is operator-driven CLI walkthrough, not automated E2E suite | Purpose is to expose gaps in the existing workflow before adding automation. A manual script against live Notion validates real handoff behavior that mocked tests cannot. The operator script doubles as a regression reference for future versions. |  | — |
| D-051 | 2026-03-19 | Ship dispatch routes through delivery-manager; implementation owns final done transition | Review acceptance is a routing signal, not proof that ship wrap-up is complete. Keeping ship dispatch as a delivery-manager handoff preserves the existing readiness check and prevents automation from moving a Feature to done before release notes exist. | Dispatch implementation directly from review acceptance (skips handoff validation), auto-set done in a post-dispatch hook (breaks implementation ownership and can mark incomplete shipments as finished). | — |
| D-054 | 2026-03-19 | Workflow gap fixes stay scanner-first with phase hooks and structured dispatch metadata | The open shakedown gaps are workflow-state and handoff problems, not a stack problem. The least risky fix is to keep StatusEntryScanner as the entry detector, add stronger phase gates, let post-dispatch hooks own routed/returned/blocked lifecycle writes, and accept optional structured command output for review and ship outcomes so backlog fields can be updated idempotently without introducing a new orchestration layer. |  | — |
| D-042 | 2026-03-20 | Release gate as new CLI subcommand | Release readiness validation is a distinct capability that fits naturally as briefcase release gate; keeps release.py as the single command group for release operations. |  | — |
| D-040 | 2026-03-20 | Group installer feedback into single brief | Four related installer bugs (error visibility, pip consistency, command ref, non-interactive) serve a single user job; grouping reduces brief bloat and enables coordinated testing. |  | — |
| D-041 | 2026-03-20 | Group Notion setup feedback into single brief | Three Notion setup items (conditional docs/plan, preflight access, README emoji) serve a single user job; grouping avoids fragmented briefs. |  | — |
| D-035 | 2026-03-20 | Timestamp surfacing approach | Notion backend: extract created_time and last_edited_time from API response (zero schema change). Local backend: use file mtime for updated_at, optional YAML frontmatter created_at field (fallback to ctime). --since filters on updated_at by default. --group-by-date requires explicit flag for backward compatibility. |  | — |
| D-050 | 2026-03-20 | Intent fidelity via Expected Experience, not multi-model | Root cause analysis showed brief ambiguity, not review model quality, drives intent mismatches. Adding a concrete Expected Experience section to briefs, a pre-build validation step, and a review intent-check addresses the upstream cause. Multi-model review would not catch briefs that are technically correct but miss UX intent. |  | — |
| D-041 | 2026-03-21 | Lane field as Notion select property | Consistent with existing schema pattern (Type, Status, Priority are all select fields). Queryable in Notion views. Upgrade command can patch existing databases. |  | — |
| D-051 | 2026-03-21 | Briefs storage: database not container page | Container pages in Notion cannot sort, filter, or group child pages. Moving briefs to a database gives structured properties (Status, Date, Linked Feature, Author), native table views with grouping by date, and query-based access instead of block scanning. Migration is a one-time operation with a dedicated CLI command. |  | — |
| D-042 | 2026-03-21 | Self-update via GitHub release tarball | No git required in consumer project. Immutable versioned snapshots. Unauthenticated GitHub API sufficient for public repo. Manifest.json tracks file hashes for customization detection. |  | — |
| D-045 | 2026-03-29 | Project metadata uses single-value field | New artifacts need a structured place for project identity that supports simple filtering and default propagation without turning into a general taxonomy system. A single-value Project field keeps the initial model clear and low-friction. |  | — |
| D-055 | 2026-03-30 | Brief lifecycle guards: validation at CLI command layer | Place promotion guards in brief_write CLI command (after merge, before store.write_brief) rather than in the storage backend or service layer. CLI layer is the single entry point for all writes, works identically on local and Notion backends, keeps pure validation logic (no I/O) in briefs.py easily unit-testable. Rejected: backend-level validation (would require duplicating logic per backend) and service-layer validation (artifact_service is a thin passthrough, adding branching there adds indirection). |  | — |
| D-056 | 2026-04-08 | Namespaced private artifact mirror | A shared private git repo with project_slug namespacing keeps planning artifacts private while allowing deterministic sync push/pull reads across consumer workspaces. |  | — |
